---

- hosts: localhost
  become: yes
  tasks:
    - user:
        name: johnd
        comment: "John Doe"

- hosts: localhost
  roles:
    - {role: ansible-sudo}

- hosts: localhost
  become: yes
  roles:
    - {role: ansible-sudo, sudo_users: [travis, johnd]}
    - {role: ansible-sudo, sudo_users: [travis, johnd, janed]}
...